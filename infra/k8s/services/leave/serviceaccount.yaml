# ============================================
# Leave Service - ServiceAccount
# ============================================

apiVersion: v1
kind: ServiceAccount
metadata:
  name: leave-sa
  namespace: default
  labels:
    app: leave
    tier: application
automountServiceAccountToken: true
