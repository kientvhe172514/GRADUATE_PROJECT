# ============================================
# Employee Service - ServiceAccount
# ============================================

apiVersion: v1
kind: ServiceAccount
metadata:
  name: employee-sa
  namespace: graduate-project
  labels:
    app: employee
    tier: application
automountServiceAccountToken: true
