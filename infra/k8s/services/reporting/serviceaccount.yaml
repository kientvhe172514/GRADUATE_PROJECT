# ============================================
# Reporting Service - ServiceAccount
# ============================================

apiVersion: v1
kind: ServiceAccount
metadata:
  name: reporting-sa
  namespace: default
  labels:
    app: reporting
    tier: application
automountServiceAccountToken: true
