# ============================================
# Notification Service - ServiceAccount
# ============================================

apiVersion: v1
kind: ServiceAccount
metadata:
  name: notification-sa
  namespace: default
  labels:
    app: notification
    tier: application
automountServiceAccountToken: true
