# ============================================
# Auth Service - Production HA Deployment
# ============================================
# Similar to Face-Recognition: anti-affinity, PDB, resources, health checks
# ============================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-depl
  namespace: graduate-project
  labels:
    app: auth
    service: auth
    tier: application
    version: v1
spec:
  replicas: 1  # Start with 1 pod, HPA will scale
  
  selector:
    matchLabels:
      app: auth
      service: auth
  
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  
  template:
    metadata:
      labels:
        app: auth
        service: auth
        tier: application
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3001"
        prometheus.io/path: "/metrics"
        sidecar.istio.io/inject: "false"
    
    spec:
      serviceAccountName: auth-sa
      
      # ===== ANTI-AFFINITY =====
      # FIX: Changed to preferredDuringScheduling to avoid scheduling deadlock
      # with replicas=1 or limited nodes. Hard requirement can block pod scheduling.
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - auth
              topologyKey: kubernetes.io/hostname
          - weight: 50
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - auth
              topologyKey: topology.kubernetes.io/zone
      
      # ===== SECURITY CONTEXT =====
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      
      containers:
      - name: auth
        image: tranvukien125/graduate-project-auth:latest
        imagePullPolicy: Always
        
        ports:
        - containerPort: 3001
          name: http
          protocol: TCP
        - containerPort: 9090
          name: metrics
          protocol: TCP
        
        env:
        # ===== POD IDENTITY =====
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: HOSTNAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        
        # ===== APP CONFIG =====
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3001"
        - name: TZ
          value: "Asia/Ho_Chi_Minh"
        
        # Mount .env file from secret (CI/CD injects all vars as .env string)
        volumeMounts:
        - name: env-file
          mountPath: /app/services/auth/.env
          subPath: .env
          readOnly: true
        - name: tmp
          mountPath: /tmp
        - name: cache
          mountPath: /app/.cache
        
        # ===== RESOURCES (OPTIMIZED FOR GRADUATION PROJECT) =====
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
            ephemeral-storage: "128Mi"
          limits:
            memory: "512Mi"
            cpu: "500m"
            ephemeral-storage: "256Mi"
        
        # ===== HEALTH CHECKS =====
        startupProbe:
          httpGet:
            path: /api/v1/auth/health
            port: 3001
            scheme: HTTP
          initialDelaySeconds: 30  # Increased to allow DNS propagation
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 12
          successThreshold: 1
        
        readinessProbe:
          httpGet:
            path: /api/v1/auth/health
            port: 3001
            scheme: HTTP
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
          successThreshold: 1
        
        livenessProbe:
          httpGet:
            path: /api/v1/auth/health
            port: 3001
            scheme: HTTP
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 3
          successThreshold: 1
        
        # ===== SECURITY =====
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 1000
          capabilities:
            drop:
            - ALL
      
      volumes:
      - name: env-file
        secret:
          secretName: auth-secrets
          items:
          - key: .env
            path: .env
      - name: tmp
        emptyDir:
          sizeLimit: 500Mi
      - name: cache
        emptyDir:
          sizeLimit: 1Gi
      
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      terminationGracePeriodSeconds: 60

---
# ===== POD DISRUPTION BUDGET =====
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: auth-pdb
  namespace: graduate-project
  labels:
    app: auth
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: auth
      service: auth

---
# ===== SERVICE ACCOUNT =====
apiVersion: v1
kind: ServiceAccount
metadata:
  name: auth-sa
  namespace: graduate-project
  labels:
    app: auth
automountServiceAccountToken: true

---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: auth-role
  namespace: graduate-project
  labels:
    app: auth
rules:
- apiGroups: [""]
  resources: ["configmaps", "secrets"]
  resourceNames: ["auth-config", "auth-secrets"]
  verbs: ["get", "watch"]
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get", "list"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: auth-rolebinding
  namespace: graduate-project
  labels:
    app: auth
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: auth-role
subjects:
- kind: ServiceAccount
  name: auth-sa
  namespace: graduate-project

---
# ===== SERVICE =====
apiVersion: v1
kind: Service
metadata:
  name: auth-srv
  namespace: graduate-project
  labels:
    app: auth
    service: auth
  annotations:
    service.alpha.kubernetes.io/tolerate-unready-endpoints: "true"
spec:
  type: ClusterIP
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  ports:
  - name: http
    port: 3001
    targetPort: 3001
    protocol: TCP
  - name: metrics
    port: 9090
    targetPort: 9090
    protocol: TCP
  selector:
    app: auth
    service: auth

---
# ===== HPA =====
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: auth-hpa
  namespace: graduate-project
  labels:
    app: auth
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: auth-depl
  
  minReplicas: 1
  maxReplicas: 2
  
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 120
      policies:
      - type: Pods
        value: 2
        periodSeconds: 60
      selectPolicy: Max
    
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
      - type: Pods
        value: 1
        periodSeconds: 120
      selectPolicy: Min
  
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 85
  
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 90


