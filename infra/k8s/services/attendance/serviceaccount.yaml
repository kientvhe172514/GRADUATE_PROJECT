# ============================================
# Attendance Service - ServiceAccount
# ============================================

apiVersion: v1
kind: ServiceAccount
metadata:
  name: attendance-sa
  namespace: default
  labels:
    app: attendance
    tier: application
automountServiceAccountToken: true
